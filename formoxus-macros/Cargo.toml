[package]
name = "formoxus-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
# `extra-traits` for `Debug` on syn types: form2.rs derives Debug across its
# parsed spec types, and its 78 parser tests assert on that output. It used to
# arrive transitively through darling; with darling gone it has to be declared.
syn = { version = "3", features = ["full", "extra-traits"] }
quote = "1"
proc-macro2 = "1"
heck = { workspace = true }

[dev-dependencies]
googletest = { workspace = true }
