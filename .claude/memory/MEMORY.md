> Paths changed 2026-09-19: the derive path was deleted and `reflect/` flattened into the crate root, so `formoxus::reflect::X` is now `formoxus::X` and `src/reflect/y.rs` is now `src/y.rs`. Entries below written before that date use the old addresses; the reasoning in them still holds. See [Derive-path removal](derive_path_removal.md).

- [Widget registry idea](widget_registry_idea.md) — **PAUSED MID-SCOPING, 2026-09-19, nothing built** — a type-keyed default-widget registry so a clean model crate can still get a custom widget without every form re-declaring it; orphan rule blocks a trait-impl version, `&'static Shape` verified as a real Hash+Eq key; open fork (global OnceLock vs threaded RenderCtx) needs Todd before building
- [Running to-do queue](next_up_two_todos.md) — wire rework FINISHED 2026-09-18: option (a) leaves-over-the-wire, Submission<T> packages the server-side boilerplate, ChangePasswordProblem gone, `cargo test --workspace` green at 476 (as measured inside apcsp-dioxus before extraction); remaining = 4 render_control arms + Path/Prefix newtype (deferred)
- [Facet form design decisions](facet_form_design_decisions.md) — why: facet vtables replace FromStr/Display, FieldSet lost its generic to enable recursion, signals stay out of the data model, uncontrolled forms need none, enum variant chosen up front and locked, typed VariantChoice + iterative disclosure, panic→Result reversed 2026-09-02, ForCreate not deferred mode, Vec rows named by index + write_value_into split, Option peels one layer per recursion, presence is a per-kind is_present NOT a leaf scan, FormMode not a recomputed seeding bool, "" IS absence at both boundaries, T->String->T pinned for every builtin scalar, Form-as-schema + Store-as-values, Unchosen replaces Absent, choose_variant dispatches by path containment, wasm panics never reach an ErrorBoundary so Result is transport, widget boundary is (path,label,required,errors)+one channel, form2! beats facet attributes because it expands in the CONSUMING crate (orphan rule dissolves), FormSpec is the declaration and FormState the built tree, Edit stays OUT of the spec, `x.y` vs `x.y[]` vs `x.y[].z`; BUTTONS built 2026-09-16 (slot struct proved impossible, using_fns! is a runtime-reconciled map); push_field_error dispatches like edit(), Form is &self/FormState is &mut self, form.rs split into form/{spec,state}.rs, prelude::Form name-collision trap, Form/FormState can never cross a server fn
- [Facet form spike](facet_form_spike.md) — the reflection path's origin story: renders real Elements via RenderCtx + one component per leaf; `Form<T>` is the reactive handle over plain-data `FormState<T>`; variant fields namespaced under `$Variant` segments; RETIRING THE DERIVE PATH — form2! parser+expand+compile-checked witness+control vocabulary+custom(Widget), FormSpec/apply_specs/`[]` row selectors, HtmlInput renders every input type, validate runs spec.validator last
- [Formoxus control survey](formoxus_control_survey.md) — Django's Field/Widget split (constraints follow the VALUE type, survive a widget swap), leptos_form has NO constraint support, facet's define_attr_grammar! verified (i64 newtypes yes, i64 struct fields NO, container-level validators rejected), all 22 input types + 8 ValueKinds, why control resolution must happen at-or-before validate; the attribute path was built, proven, then abandoned for a proc macro
- [Facet newtypes & custom widgets](facet_newtypes_and_custom_widgets.md) — ControlType::Custom built (fn ptr not Box<dyn Fn>, hand-written Debug/PartialEq); newtypes are LEAVES (one-field TupleStruct + scalar inside, NOT any one-field struct — leaf paths ARE the wire format); newtype scalar_type()=None, parse_from_str FAILS even with #[facet(transparent)], use begin_nth_field(0); FormField carries the INNER type plus a `wrapper: Option<&'static Shape>` side-channel
- [Formoxus roadmap](formoxus_roadmap.md) — MVP status as of 5cd1b40 (STALE in large part — predates the reflection-path rewrite; kept for the derive-path history it records)
- [Formoxus feature parity](formoxus_feature_parity.md) — vs. Django forms/leptos_form as of 606aa76: ahead on Providers, roughly at parity on repeating groups/FieldSet, gaps in error-render config, field attrs, live validation, file upload; leptos_form's Vec support has render-time min/max bounds and STABLE row ids, not index-named rows like ours
- [Formoxus/darling gotchas](formoxus_darling_gotchas.md) — Option<T> needs no #[darling(default)]; Vec<T> needs #[darling(multiple)]; SpannedValue<T> for spans; quote!/rsx! nesting traps; custom enum FromMeta needs quoted strings; rsx child splices need {} unless always-literal
- [facet 0.50 compatibility](facet_050_compatibility.md) — verified 2026-09-19 by full-workspace build: 0.50.0-rc.7 is a DROP-IN (one version string, zero code changes, 359 tests green); the 0.47-0.49 gap is not a reflection-core rewrite — facet-reflect's public surface is 359 fns unchanged, facet-core purely additive, `Shape` Hash/Eq-on-id byte-identical (so the widget-registry key premise holds); 0.50 = facet-path + taxon_bridge + namespaced attrs; STAY on 0.46.5, this makes the stable-release bump a one-liner
- [Derive-path removal](derive_path_removal.md) — 2026-09-19: the derive path DELETED and `reflect` FLATTENED to the crate root (`formoxus::Form`, not `formoxus::reflect::Form`); the two paths shared only error/label_case/one 11-line FieldErrors component, and form2! shared nothing; three traps — darling was silently supplying `syn/extra-traits`, the macros emit absolute `::formoxus::reflect::` paths that only the consumer test target resolves (one assertion hard-codes them token-spaced), and tests/ui/ held 4 derive goldens that fail as golden MISMATCHES; formoxus-macros now non-optional, tests/reflect.rs → tests/consumer.rs; new baseline 317
