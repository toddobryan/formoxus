> Paths changed 2026-09-19: the derive path was deleted and `reflect/` flattened into the crate root, so `formoxus::reflect::X` is now `formoxus::X` and `src/reflect/y.rs` is now `src/y.rs`; `form!` is now `form!`. Entries below written before that date use the old addresses; the reasoning in them still holds. See [Derive-path removal](derive_path_removal.md).

- [Widget registry idea](widget_registry_idea.md) — **PAUSED MID-SCOPING, 2026-09-19, nothing built** — a type-keyed default-widget registry so a clean model crate can still get a custom widget without every form re-declaring it; orphan rule blocks a trait-impl version, `&'static Shape` verified as a real Hash+Eq key; open fork (global OnceLock vs threaded RenderCtx) needs Todd before building
- [Running to-do queue](next_up_two_todos.md) — wire rework FINISHED 2026-09-18: option (a) leaves-over-the-wire, Submission<T> packages the server-side boilerplate, ChangePasswordProblem gone, `cargo test --workspace` green at 476 (as measured inside apcsp-dioxus before extraction); remaining = 4 render_widget arms + Path/Prefix newtype (deferred)
- [Facet form design decisions](facet_form_design_decisions.md) — why: facet vtables replace FromStr/Display, FieldSet lost its generic to enable recursion, signals stay out of the data model, uncontrolled forms need none, enum variant chosen up front and locked, typed VariantChoice + iterative disclosure, panic→Result reversed 2026-09-02, ForCreate not deferred mode, Vec rows named by index + write_value_into split, Option peels one layer per recursion, presence is a per-kind is_present NOT a leaf scan, FormMode not a recomputed seeding bool, "" IS absence at both boundaries, T->String->T pinned for every builtin scalar, Form-as-schema + Store-as-values, Unchosen replaces Absent, choose_variant dispatches by path containment, wasm panics never reach an ErrorBoundary so Result is transport, widget boundary is (path,label,required,errors)+one channel, form! beats facet attributes because it expands in the CONSUMING crate (orphan rule dissolves), FormSpec is the declaration and FormState the built tree, Edit stays OUT of the spec, `x.y` vs `x.y[]` vs `x.y[].z`; BUTTONS built 2026-09-16 (slot struct proved impossible, using_fns! is a runtime-reconciled map); push_field_error dispatches like edit(), Form is &self/FormState is &mut self, form.rs split into form/{spec,state}.rs, prelude::Form name-collision trap, Form/FormState can never cross a server fn
- [Facet form spike](facet_form_spike.md) — the reflection path's origin story: renders real Elements via RenderCtx + one component per leaf; `Form<T>` is the reactive handle over plain-data `FormState<T>`; variant fields namespaced under `$Variant` segments; RETIRING THE DERIVE PATH — form! parser+expand+compile-checked witness+widget vocabulary+custom(Widget), FormSpec/apply_specs/`[]` row selectors, HtmlInput renders every input type, validate runs spec.validator last
- [Formoxus widget survey](formoxus_widget_survey.md) — Django's Field/Widget split (constraints follow the VALUE type, survive a widget swap), leptos_form has NO constraint support, facet's define_attr_grammar! verified (i64 newtypes yes, i64 struct fields NO, container-level validators rejected), all 22 input types + 8 ValueKinds, why widget resolution must happen at-or-before validate; the attribute path was built, proven, then abandoned for a proc macro
- [Facet newtypes & custom widgets](facet_newtypes_and_custom_widgets.md) — WidgetType::Custom built (fn ptr not Box<dyn Fn>, hand-written Debug/PartialEq); newtypes are LEAVES (one-field TupleStruct + scalar inside, NOT any one-field struct — leaf paths ARE the wire format); newtype scalar_type()=None, parse_from_str FAILS even with #[facet(transparent)], use begin_nth_field(0); FormField carries the INNER type plus a `wrapper: Option<&'static Shape>` side-channel
- [Formoxus roadmap](formoxus_roadmap.md) — MVP status as of 5cd1b40 (STALE in large part — predates the reflection-path rewrite; kept for the derive-path history it records)
- [Formoxus/darling gotchas](formoxus_darling_gotchas.md) — Option<T> needs no #[darling(default)]; Vec<T> needs #[darling(multiple)]; SpannedValue<T> for spans; quote!/rsx! nesting traps; custom enum FromMeta needs quoted strings; rsx child splices need {} unless always-literal
- [facet 0.50 compatibility](facet_050_compatibility.md) — verified 2026-09-19 by full-workspace build: 0.50.0-rc.7 is a DROP-IN (one version string, zero code changes, 359 tests green); the 0.47-0.49 gap is not a reflection-core rewrite — facet-reflect's public surface is 359 fns unchanged, facet-core purely additive, `Shape` Hash/Eq-on-id byte-identical (so the widget-registry key premise holds); 0.50 = facet-path + taxon_bridge + namespaced attrs; STAY on 0.46.5, this makes the stable-release bump a one-liner
- [Derive-path removal](derive_path_removal.md) — 2026-09-19: the derive path DELETED and `reflect` FLATTENED to the crate root (`formoxus::Form`, not `formoxus::reflect::Form`); the two paths shared only error/label_case/one 11-line FieldErrors component, and form! shared nothing; three traps — darling was silently supplying `syn/extra-traits`, the macros emit absolute `::formoxus::reflect::` paths that only the consumer test target resolves (one assertion hard-codes them token-spaced), and tests/ui/ held 4 derive goldens that fail as golden MISMATCHES; formoxus-macros now non-optional, tests/reflect.rs → tests/consumer.rs; new baseline 317
- [Widget table & what a Choice is](widget_table_and_choice.md) — **NEXT UP, queued 2026-09-19, nothing built** — only 45/84 (value kind, widget) pairs render and the other 39 fail by the field SILENTLY VANISHING (Dioxus contains the panic to that component, so catch_unwind sees nothing); NOT a missing-match-arm job — `ValueKind::Choice`/`MultiChoice`/`File` are never constructed, so there is nothing to match; the fork needing Todd is SHAPE choice (built: VariantSet/VariantSelect) vs VALUE choice (absent; the real Django/leptos_form parity gap, probably entangled with Provider); also records the deferred doc pass (179 items)
- [Choice fields design](choice_fields_design.md) — **TIER 1 BUILT 2026-09-21** (`widget: select { choices: EXPR }` — new widget-argument brace block, `FieldSpec.choices` not a `WidgetType` payload, `SelectChoice: From<&(&str,&str)>` so a `const` table works, `(Text,Select)` renders at last); tiers 2–3 still open — Todd's 2026-09-19 design for VALUE-choice fields: `Choice { display, raw_value }` reusing the normal parse path (raw_value may NEVER be `""` — that IS absence), choices attach to the WIDGET not the type (so `ValueKind::Choice`/`MultiChoice` get DELETED, not filled in), shape-choice stays separate from value-choice; OPEN DECISION: a `Box<dyn Fn>` cannot live on `WidgetType` (kills Clone+Debug+PartialEq, the same wall `Custom` hit) so either a `fn(&ValuesByPath) -> Vec<Choice>` pointer + `Provider` for the async case, or one capturing closure in a path-keyed side-channel; ALSO: Select/RadioGroup are easy, SelectMultiple/CheckboxMultiple are a structural problem (a leaf holds ONE string) — ship single-choice first
- [Config cascade](config_cascade.md) — **DIRECTION AGREED 2026-09-19, nothing built** — `novalidate`, `label_case` and the widget registry all want ONE mechanism: **context default → per-form → per-field**; use Dioxus CONTEXT (`try_use_context` in the leaf component) rather than a global OnceLock or threading RenderCtx — no signature changes, properly scoped, and `Option` makes the server path safe since it rebuilds but never renders; SUPERSEDES the open fork in widget-registry-idea; checked leptos_form 0.2.0-rc1 at source: two tiers only (container→field), `rename_all` struct-level ONLY, NO global tier anywhere — and formoxus is currently BEHIND it, `LabelCase::Title` is hardcoded at members.rs:144; also records WHY: formoxus sets no `novalidate` so the browser's validation pre-empts its own, making its error rendering largely unreachable
- [Widget is the umbrella word](widget_is_the_umbrella_word.md) — 2026-09-20: ONE word, `widget`, for anything that renders a field; REVERSES the widget→control rename of the day before (1cd4069) because nothing here renders a bare element — even `Input` wraps a label, caption, required marker and error list; Django/Flutter/Qt all use widget as the umbrella; a Control-vs-Widget split was rejected (it cuts a different axis than `WidgetType`'s built-in-vs-`Custom`, and `RadioGroup` is already a built-in composite); the word is REUSED from the derive path's traits, not inherited
- [Const shape walk blocked](const_shape_walk_blocked.md) — **CORRECTED AGAIN 2026-09-25: a `const {}` in a generic fn only fires when monomorphized, so never under `cargo check` and never in the uncalled `__paths_exist`; step 6a shipped as FREE `const _` items + `shape_of(|m: &Model| &m.field)` in `formoxus::field_kind`** — CONCLUSION REVERSED 2026-09-21 — an inline `const {}` in a generated generic fn sees BOTH `T::SHAPE` and a literal baked into it, which closes the constraint-vs-value-kind check AND the bound-fits-the-type check at compile time, with no const generics; only CROSSING a `ShapeRef` to a field's type is blocked, and classification never does that; beats marker traits on three counts (one source of truth with `value_kind()`, can say "can't tell" for a newtype, no orphan problem); `pattern` still needs a `LitStr` because regex parsing allocates
- [Pre-publish checklist](pre_publish_checklist.md) — **RAISE WHEN PUBLISHING COMES UP** (Todd asked, 2026-09-21) — nothing runs `wasm-opt`, so every measured wasm size IS the shipped size and a 10–30% win is going unclaimed; plus the 186-item doc pass, which facet version to release against, and verifying the macro-side `regress` never reaches the bundle
- [Error model design](error_model_design.md) — **2026-09-21, NOTHING BUILT** — no custom check can attach to a field today (`FormError` has no path); DECIDED: merge the two message types, keep storage POSITIONAL (a member IS its path, and `Path<T>` is generic so a stored path would be an untyped `String`), put the path on the PRODUCER as `Verdict<T>` where `T` is still in scope; per-field validators use the `fn`-pointer + `&dyn Any` + witness-pairing trick; `ErrorsByPath` agreed but DEFERRED — it fixes a real reactivity asymmetry (errors arrive as props from a whole-tree `state.read()`) but is a perf change whose payoff needs live validation, and `FormState` must stay the authority because `validate` runs server-side with no runtime
- [Lint config](lint_config.md) — **RAISE PERIODICALLY** (Todd asked, 2026-09-23) — `unused_qualifications` is deliberately OFF because it fires inside `rsx!` expansions and blames the attribute name (rustc's suggested fix for `onsubmit: move |e|` is to delete `onsubmit:`); sweep for new REAL ones with `-W unused_qualifications` and ignore every hit sitting on a `move |…|`; also records that `mod_module_files`/`self_named_module_files` are named for the layout they REJECT, not the one they enforce, and what else is off and why
- [facet re-export needs a direct dep](facet_reexport_needs_direct_dep.md) — 2026-09-25: `pub use facet` is for the MACRO's generated code (6a); users STILL need `facet` directly, because the derive hard-codes `::facet` and `#[facet(crate = ::formoxus::facet)]` does not cover builtin attrs like `transparent` (fix is facet-rs/facet#2662, pending)

- [Formoxus feature parity](formoxus_feature_parity.md) — RE-SURVEYED 2026-09-25 vs Django + leptos_form (unmaintained since Feb 2024): ahead on compile-time checks/enums/one validation; gaps = 4 widgets form! accepts but can't render, per-field validators, typed dates/decimals/uuids, help text + widget attrs, change tracking, formset bounds, undocumented grammar
