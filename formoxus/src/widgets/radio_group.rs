use dioxus::prelude::*;

use crate::{ValuesByPath, widgets::{FieldProps, SelectChoice}};



#[component]
pub fn RadioGroup(values: ValuesByPath, choices: Vec<SelectChoice>, props: FieldProps) -> Element {
    todo!()
}